## What this changes

<!-- One or two sentences: what does this PR do, and why? -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] New engine (manifest + browser adapter)
- [ ] Docs only
- [ ] Other (describe above)

## Checklist

- [ ] `npm run typecheck` passes for both workspaces
- [ ] `npm run build` passes for both workspaces
- [ ] `npm test` passes locally
- [ ] If this touches a database engine's behavior, I added a real end-to-end test in `control-plane/src/__tests__/engines.integration.test.ts` (not just a mocked/unit one) — see `CONTRIBUTING.md`
- [ ] I checked CI actually ran green on this branch, not just locally

## Anything reviewers should look at closely

<!-- Optional: a tricky bit of logic, a design decision you're unsure about, scope you deliberately cut. -->
